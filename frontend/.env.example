# Frontend crawler client mode
VITE_CRAWL_CLIENT_MODE=mock
VITE_CRAWL_TRANSPORT=sse

# Leave blank for same-origin deployments.
# For local split frontend/backend dev, set backend URL explicitly.
VITE_CRAWL_API_BASE_URL=
VITE_CRAWL_API_PREFIX=/api/crawl
